name: Build and deploy Python app to Azure Web App - CVReformat

on:
  push:
    branches: [ master, Development ]
  workflow_dispatch:
    inputs:
      target:
        description: "Which environment to deploy to?"
        required: false
        default: "develop"   # so manual runs go to dev first
        type: choice
        options:
          - develop
          - master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Create venv and install deps
        run: |
          python -m venv venv
          source venv/bin/activate
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
      - name: Zip artifact for deployment
        run: zip -r release.zip . -x "venv/*"
      - uses: actions/upload-artifact@v4
        with:
          name: python-app
          path: release.zip

  # ---------- DEV DEPLOY ----------
  deploy-dev:
    # run if:
    # 1) this was a push to develop  OR
    # 2) this was a manual run and user chose "develop"
    if: >
      github.ref == 'refs/heads/develop' ||
      (github.event_name == 'workflow_dispatch' && github.event.inputs.target == 'develop')
    runs-on: ubuntu-latest
    needs: build
    environment: develop
    permissions:
      id-token: write
      contents: read
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: python-app
          path: .
      - name: Azure login (Dev)
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID_DEV }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID_DEV }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID_DEV }}
      - name: Deploy to Azure Web App (Dev)
        uses: azure/webapps-deploy@v3
        with:
          app-name: cv-reformatting-tool-dev
          package: release.zip

  # ---------- PROD DEPLOY ----------
  deploy-prod:
    # run if:
    # 1) this was a push to master  OR
    # 2) this was a manual run and user chose "master"
    if: >
      github.ref == 'refs/heads/master' ||
      (github.event_name == 'workflow_dispatch' && github.event.inputs.target == 'master')
    runs-on: ubuntu-latest
    needs: build
    environment: Production
    permissions:
      id-token: write
      contents: read
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: python-app
          path: .
      - name: Azure login (Prod)
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
      - name: Deploy to Azure Web App (Prod)
        uses: azure/webapps-deploy@v3
        with:
          app-name: cv-reformatting-tool
          package: release.zip

